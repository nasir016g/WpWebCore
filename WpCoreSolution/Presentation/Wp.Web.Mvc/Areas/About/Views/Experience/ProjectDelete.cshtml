@model Wp.Web.Mvc.About.Models.ProjectModel

@{
    ViewBag.Title = "ProjectDelete";
}
@{
    ViewBag.Title = "Delete";
}
<h3>Are you sure you want to delete this?</h3>
   
<div class="row">
    <div class="col-md-1">
        @Html.DisplayNameFor(model => model.Name)
</div>
<div class="col-md-2">
    @Html.DisplayFor(model => model.Name)
</div>
</div>
    

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        @Html.HiddenFor(model => model.ExperienceId)
        <input type="submit" class="btn btn-default" value="Delete" />
        @Html.ActionLink("Back to List", "ItemIndex", new { experienceId = Model.ExperienceId }, new { @class = "btn btn-default" })
    </p>
}
